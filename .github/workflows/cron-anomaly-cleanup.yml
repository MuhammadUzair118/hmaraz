name: Cron Anomaly Cleanup

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger anomaly cleanup cron
        run: |
          curl -s -X GET "https://hamraz-web.vercel.app/api/cron/anomaly-cleanup" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            -w "\nHTTP %{http_code}"
